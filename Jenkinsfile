pipeline {
    agent {
        label 'java-label'
    }
    stages {
        stage('Build') {
            steps {
                echo "Welcome to tools demo"
                sh 'mvn --version'
            }
        }
    }
}
